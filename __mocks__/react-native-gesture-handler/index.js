import { View, TouchableWithoutFeedback } from 'react-native';

const Swipeable = View;
const DrawerLayout = View;
const ScrollView = View;
const Slider = View;
const Switch = View;
const TextInput = View;
const ToolbarAndroid = View;
const ViewPagerAndroid = View;
const DrawerLayoutAndroid = View;
const WebView = View;
const NativeViewGestureHandler = View;
const TapGestureHandler = View;
const FlingGestureHandler = View;
const ForceTouchGestureHandler = View;
const LongPressGestureHandler = View;
const PanGestureHandler = View;
const PinchGestureHandler = View;
const RotationGestureHandler = View;
const BaseButton = TouchableWithoutFeedback;
const BorderlessButton = TouchableWithoutFeedback;
const attachGestureHandler = () => {};
const createGestureHandler = () => {};
const dropGestureHandler = () => {};
const updateGestureHandler = () => {};
const Direction = {
  RIGHT: 1,
  LEFT: 2,
  UP: 4,
  DOWN: 8,
};
const State = {
  BEGAN: 'BEGAN',
  FAILED: 'FAILED',
  ACTIVE: 'ACTIVE',
  END: 'END',
  UNDETERMINED: 'UNDETERMINED',
};
/* Buttons */
const RawButton = View;
const RectButton = View;
/* Other */
const FlatList = View;
const gestureHandlerRootHOC = jest.fn();
const Directions = {};

export {
  Swipeable,
  DrawerLayout,
  ScrollView,
  Slider,
  Switch,
  TextInput,
  ToolbarAndroid,
  ViewPagerAndroid,
  DrawerLayoutAndroid,
  WebView,
  NativeViewGestureHandler,
  TapGestureHandler,
  FlingGestureHandler,
  ForceTouchGestureHandler,
  LongPressGestureHandler,
  PanGestureHandler,
  PinchGestureHandler,
  RotationGestureHandler,
  BaseButton,
  BorderlessButton,
  attachGestureHandler,
  createGestureHandler,
  dropGestureHandler,
  updateGestureHandler,
  Direction,
  State,
  /* Buttons */
  RawButton,
  RectButton,
  /* Other */
  FlatList,
  gestureHandlerRootHOC,
  Directions,
};
