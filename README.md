### fiddling with react native

## Stuff that works

* naviagtion stack :o
* form input
* custom validations
* tests :o
* mocked login ;)

For brave visitors:

`yarn install`

To run on android emulator
`react-native run-android`

To run on ios emulator
`cd ios && pod install` (you need to have CocoaPods)
`cd ..`
`react-native run-ios`
